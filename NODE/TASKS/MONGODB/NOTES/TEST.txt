1️⃣ What is JWT and why is it used in backend applications?

JWT (JSON Web Token) is a secure token used for authentication and authorization between client and server.

It is used to:

Verify user identity after login

Secure protected routes

Transfer user data safely

Avoid storing session in server (stateless)

Example use: Login → server sends JWT → client uses JWT for future requests

2️⃣ What are the three parts of a JWT token?

JWT has 3 parts separated by dots (.)

header.payload.signature

Example:

eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9
.
eyJpZCI6IjEyMyIsIm5hbWUiOiJBYmlzaGVrIn0
.
abc123xyz456

Parts:

Header

Payload

Signature

3️⃣ What is the difference between payload and signature in JWT?

Payload

Contains user data

Example:

{
  id: "123",
  email: "user@gmail.com"
}

Signature

Used to verify token authenticity

Created using:

header + payload + secret key

Payload = data
Signature = security verification

4️⃣ What is the purpose of jwt.sign()?

Used to create/generate a JWT token

Example:

jwt.sign({ id: user._id }, process.env.JWT_SECRET, { expiresIn: "1h" })

Purpose:

Encode user data

Create secure token

Add expiration

5️⃣ What is the purpose of jwt.verify()?

Used to verify token validity

Example:

jwt.verify(token, process.env.JWT_SECRET)

Purpose:

Check token is valid

Check token is not expired

Decode payload

6️⃣ Why should JWT secret keys be stored in .env file?

For security reasons

Reasons:

Prevent secret exposure

Protect from hackers

Avoid hardcoding in source code

Safe in production

Example:

JWT_SECRET=mysecretkey
7️⃣ What happens if the JWT secret used for verification is different from the one used for signing?

Verification will fail

Error example:

invalid signature

Result:

Token rejected

User cannot access protected routes

8️⃣ What does the expiresIn option do in JWT?

Sets token expiration time

Example:

expiresIn: "1h"

Meaning:
Token valid for 1 hour

After expiration → user must login again

9️⃣ How do you send a JWT token from backend to client?

Usually sent in response

Example:

res.json({
  token: token
})

or

res.status(200).json({ token })
🔟 Where should the client send the JWT token when accessing protected routes?

Client sends token in Authorization header

Example:

Authorization: Bearer <token>

Example in fetch:

headers: {
  Authorization: `Bearer ${token}`
}
1️⃣1️⃣ What status code should be returned if token is missing?

401 Unauthorized

Example:

res.status(401).json({ message: "Token missing" })
1️⃣2️⃣ What status code should be returned if token is invalid or expired?

403 Forbidden or 401 Unauthorized

Common practice:

Invalid → 403

Missing → 401

Expired → 401

Example:

res.status(403).json({ message: "Invalid token" })
1️⃣3️⃣ What is the difference between Authentication and Authorization in JWT-based systems?

Authentication
= Verifying who the user is

Example:
Login → create JWT

Authorization
= Checking what the user can access

Example:
Access admin route only if role = admin

Authentication = identity
Authorization = permissions

1️⃣4️⃣ Why is JWT called a stateless authentication method?

Because server does not store session

All user data stored in token

Server only verifies token

No session storage required

Benefits:

Fast

Scalable

No memory usage for sessions

1️⃣5️⃣ How do you protect a route using JWT in Express?

Using middleware

Example:

import jwt from "jsonwebtoken";

const authMiddleware = (req, res, next) => {
    const header = req.headers.authorization;

    if (!header) {
        return res.status(401).json({ message: "Token missing" });
    }

    const token = header.split(" ")[1];

    try {
        const decoded = jwt.verify(token, process.env.JWT_SECRET);
        req.user = decoded;
        next();
    } catch (error) {
        res.status(403).json({ message: "Invalid token" });
    }
};

Protected route:

app.get("/profile", authMiddleware, (req, res) => {
    res.json({ message: "Protected route", user: req.user });
});