.ENV => eNVIRONMENTAL VARIABLES
CREATE PHASE:
* PROJECT SETUP => .ENV

IF INNTERVIEW TELL TO RUN THE PROJECT AND HE /SHE DELETE NODE MODULES AND 
FIRST WE WAN TO INSTALL => NODE MODULE => NPM I = THIS THE COMMAND TO INSTALL THE NODE MODULE AFTER 
THAT YOU CAN RUN THE PROJHECT AND NPM RUN DEV = IT WILL RUN THE PROJECT


REACT => COMPONENET BASED ARCHITECTURE 
DIFFING ALGORITHM
RECONSILLATION
VITRUTAL DOM



FIRST COMPONENT AND PAGES
INSIDE PAGES => ADMIN ,CLIENT,


Diffing Algorithm: An algorithm that compares the previous and current Virtual DOM trees to identify minimal changes.
Reconciliation: The process of efficiently updating the real DOM based on the diffing results.


CREATE PHASE 
1.COMPONENTS - SMALL AND LARGER UI AND LOGIC
TWO METHODS ; CLASS AND FUNCTIONAL COMPONENT
CLASS IS OLD METHODS , LIFECYCLE METHODS
FUNCTIONAL => BECAUSE OF HOOKS AND WE SHOLD WRITE SINGLE LINE CODE
FUNCTION = FUNCTION EXPRESSION , ARROW FUNCTION, NORMAL FUNCTION

2.NAMED EXPORT AND EXPORT DEFAULT
CONST APP =()=> {
    RETURN (
        <>
        </>
    )
}
EXPORT DEFAULT APP    => EXPORT DEFAULT 

RENDERING METHOD DOUBT =>JSX - JS -V8 ENGINE - BROWSER
3. USE STYLE
STYLE ={{BACKGROUND-COLOUR:""}}
4.CSS AND IMAGES ADDING
5. SINGLEPAGE AND MULTIPE PAGES COMPONENTS
6.RENDERING METHODS -> PRIMITIVE AND NON PRIMITIVE
ARRAY HANDLING , OBJECT HANDLING AND ARRAY OF OBJECT
7.CONDTIONAL RENDERING
8.OPTIONAL RENDERING
9.NULISH COLLIESSION
10.REACT-ROUTER-DOM
BROWSER ROUTER
ROUTES
ROUTE
LINK 
NAVLINKS
11.HOOKS
USE NAVIGATE()
OUTLET
USEPARAMS()
USE LOCATION()
